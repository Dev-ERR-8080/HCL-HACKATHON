server:
  port: 8081

spring:
  application:
    name: auth-service

  datasource:
    url: jdbc:mysql://localhost:3306/auth_db
    username: root
    password: Reddy@2003PP
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: pratapanivishnu@gmail.com
    password: lestsxyxlkmgskro
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 1071737532862-fggvu33pto7paju1vife4k4cvoqn2vf4.apps.googleusercontent.com
            client-secret: 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
            scope:
              - email
              - profile
            # ✅ FIXED: redirect-uri points to GATEWAY (8080), not auth-service (8081).
            #    Flow: user clicks Google login on frontend (3000)
            #    → browser goes to localhost:8080/oauth2/authorization/google (gateway)
            #    → gateway proxies to auth-service which redirects to Google
            #    → Google redirects back to localhost:8080/login/oauth2/code/google (gateway)
            #    → gateway proxies to auth-service which sets the JWT cookie
            #    → cookie is set for localhost:8080 — same origin as all API calls
            #    → cookie is sent on every subsequent request to localhost:8080 ✓
            redirect-uri: "http://localhost:8080/login/oauth2/code/google"

jwt:
  secret: mysecretkeymysecretkeymysecretkey12

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
    lease-renewal-interval-in-seconds: 10
    lease-expiration-duration-in-seconds: 20