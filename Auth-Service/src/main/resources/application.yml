server:
  port: 8081   # ← change to whatever port your auth service uses

spring:
  application:
    name: auth-service   # ✅ required for Eureka registration

  datasource:
    url: jdbc:mysql://localhost:3306/auth_db
    username: root
    password: Reddy@2003PP

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: pratapanivishnu@gmail.com
    password: lestsxyxlkmgskro
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 1071737532862-fggvu33pto7paju1vife4k4cvoqn2vf4.apps.googleusercontent.com
            client-secret: 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
            scope:
              - email
              - profile

# ✅ JWT secret — must match exactly with gateway's jwt.secret
jwt:
  secret: mysecretkeymysecretkeymysecretkey12   # 32+ chars required for HS256

# ✅ Eureka client config — registers this service with Eureka server
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true